<%= form_for(@mou_list) do |f| %>
  <% if @mou_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mou_list.errors.count, "error") %> prohibited this mou_list from being saved:</h2>

      <ul>
      <% @mou_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :標題 %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :內容 %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit "送出" %>
  </div>
<% end %>
