<% @mou_lists.each do |mou_list| %>
  <div class="index_row clearfix">
    <h2 class="mou_list_title"><%= link_to mou_list.title, mou_list %></h2>
    <p class="mou_list_sub_title"><%= mou_list.description %></p>
  </div>
<% end %>

<div class="links">
  <%= link_to "新增備忘錄", new_mou_list_path %>
</div>